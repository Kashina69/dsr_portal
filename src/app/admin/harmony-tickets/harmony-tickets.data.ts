import type { HarmonyTicket } from "./harmony-tickets.types";

export const MOCK_HARMONY_TICKETS: HarmonyTicket[] = [
    // Empty array to show "No record found" as per the screenshot, or we could add mock data.
    // Leaving it empty for now.
];
